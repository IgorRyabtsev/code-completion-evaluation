plugins {
    id 'java'
}

group 'com.intellij.completion.evaluation'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':antlr-parser')
    compile project(':completion-plugin')
    compile project(':interpretator')
}
